
const initialState={
    isLoading: false,
    isLogIn: false,
    error: null
}

const userReducer = (state=initialState, action)=>{
    switch(action.type){

    }
    return state;
}

export default userReducer;